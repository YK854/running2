<h4>投稿一覧</h4>
<p>
  新規投稿画面へようこそ!<br>
  まだ、新規投稿がない場合は
  <%= link_to 'こちらから', new_photo_path %>
  新規投稿をお願いします！
</p>
<table>
  <tr>
    <th>投稿ID</th>
    <th>投稿者名</th>
    <th>投稿写真</th>
    <th>タイトル</th>
    <th>説明文</th>
  </tr>
  <% @photos.each do |photo| %>
    <tr>
      <th><%= link_to photo.id, photo_path(photo.id) %></th>
      <th><%= link_to photo.user.name, user_path(photo.user.id) %></th>
      <th>
        <%= link_to photo_path(photo.id) do %>
          <%= attachment_image_tag photo, :image, :fill, 100, 100 %>
        <% end %>
      </th>
      <th><%= link_to photo.title, photo_path(photo.id) %></th>
      <th><%= link_to photo.body, photo_path(photo.id) %></th>
      <% if photo.user == current_user %>
        <th><%= link_to '削除', photo_path(photo.id), method: :delete %></th>
      <% end %>
    </tr>
  <% end %>
</table>